#include <stdio.h>

int funcao(void) {
    return 1;
}

int canScratch(int position, char* board) {
    puts("canScratch");
    printf("%d\n", position);
    // printf("%c\n", board[position]);

    return 1;
}